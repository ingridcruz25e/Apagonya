import { Component, OnInit } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { CommonModule } from '@angular/common'; import { MatCardModule } from '@angular/material/card'; import { MatButtonModule } from '@angular/material/button'; import { MatInputModule } from '@angular/material/input'; import { ReportService } from '../../services/report.service'; import { PowerOutageReport } from '../../models/report.model';
@Component({selector:'app-technician',standalone:true,imports:[CommonModule,FormsModule,MatCardModule,MatButtonModule,MatInputModule],templateUrl:'./technician.html',styleUrl:'./technician.css'})
export class TechnicianComponent implements OnInit {reports:PowerOutageReport[]=[];cause='';detail='';hora='';constructor(private api:ReportService){}ngOnInit(){this.load()}load(){this.api.assigned().subscribe({next:r=>this.reports=r,error:()=>alert('No se pudieron cargar los reportes asignados.')})}resolve(r:PowerOutageReport){this.api.resolve(r.id,{causa:this.cause,horaRestablecimiento:this.hora||new Date().toISOString(),detalle:this.detail}).subscribe({next:()=>{this.cause='';this.detail='';this.hora='';this.load();},error:e=>alert(e.error?.message||'No se pudo resolver')})}}
