import { Component, OnInit } from '@angular/core'; import { Router, RouterLink } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { CommonModule } from '@angular/common'; import { AuthService } from '../../services/auth.service'; import { ReportService } from '../../services/report.service'; import { PowerOutageReport } from '../../models/report.model';
@Component({selector:'app-dashboard',standalone:true,imports:[CommonModule,RouterLink,MatButtonModule,MatCardModule],templateUrl:'./dashboard.html',styleUrl:'./dashboard.css'})
export class DashboardComponent implements OnInit { reports:PowerOutageReport[]=[]; role='ciudadano'; error=''; constructor(private auth:AuthService,private reportsApi:ReportService,private router:Router){} ngOnInit(){this.role=localStorage.getItem('role')||'ciudadano';this.reportsApi.mine().subscribe({next:r=>this.reports=r,error:()=>this.error='No se pudieron cargar tus reportes.'});} logout(){this.auth.logout();this.router.navigate(['/login']);} confirm(id:string){this.reportsApi.confirm(id).subscribe({next:r=>{this.reports=this.reports.map(x=>x.id===r.id?r:x)},error:e=>alert(e.error?.message||'No se pudo confirmar')});}}
